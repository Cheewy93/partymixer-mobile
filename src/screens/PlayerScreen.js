import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const PlayerScreen = (props) => {
    return(
        <View>
            <Text>PlayerScreen</Text>
        </View>
    )
}

export default PlayerScreen;