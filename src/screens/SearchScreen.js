import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const SearchScreen = (props) => {
    return(
        <View>
            <Text>SearchScreen</Text>
        </View>
    )
}

export default SearchScreen;